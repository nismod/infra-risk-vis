"""
The gem_earthquake dataset has no custom rules defined.

Unfortunately the raw files cannot be programmatically fetched, they must be
placed in raster/raw/gem_earthquake/ manually prior to running snakemake.
"""