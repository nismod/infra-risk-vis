import { makeConfig } from '@/lib/helpers';

export const REGION_STYLES = makeConfig([
  {
    id: 'boundaries',
    label: 'Boundaries',
  },
  {
    id: 'population',
    label: 'Population',
  },
]);
