import { makeConfig } from 'lib/helpers';

export const NETWORK_STYLES = makeConfig([
  {
    id: 'type',
    label: 'Asset type',
  },
  {
    id: 'direct-damages',
    label: 'Direct damages',
  },
]);
